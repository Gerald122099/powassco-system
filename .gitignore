# dependencies
node_modules/

# secrets / environment
.env
.env.local
.env.*.local
.env.staging
.env.production
client/.env
client/.env.staging
client/.env.production
server/.env
server/.env.staging
server/.env.production

# keystores / certs (keep these out of the repo)
jkspowasssco
*.jks
*.keystore
*.p12
*.pfx
# Local secrets directory — Android signing key, AAB bundle, key-info,
# anything else that must NEVER leak into git.
secrets/
*.aab
signing-key-info.txt

# photoshop recovery / scratch files
*-Recovered.psd

# build output
dist/
dist-ssr/

# logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
