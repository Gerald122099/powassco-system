{
  "name": "powassco-system",
  "version": "1.0.0",
  "private": true,
  "description": "Root launcher for the POWASSCO server (monorepo). Lets PaaS hosts that build from the repo root install and run the Express API in server/.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "npm start --prefix server",
    "seed": "npm run seed --prefix server",
    "postinstall": "npm install --prefix server"
  }
}
